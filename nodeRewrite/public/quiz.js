
var Quiz = function(title) {
  console.log("But are we in this quiz file tho?");
  //We are, but only when we run the 'mocha' task...
  return {title: title};
};


module.exports = Quiz;

//module is not defined